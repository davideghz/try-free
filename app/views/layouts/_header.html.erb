<nav id="navbar" class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button href="#menu-toggle" class="slidebar-toggle" id="menu-toggle">
      <span class="sr-only">Toggle sidebar</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-123">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <span><%= link_to('TryFree', root_path, class: 'navbar-brand') %></span>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="navbar-collapse-123">
    <div class="container-fluid">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle dropdown-toggle-hide" data-toggle="dropdown"> <%= current_user.email %> <b class="caret"></b></a>
              <ul class="dropdown-menu dropdown-menu-hide">
                <li><%= link_to('<i class="glyphicon glyphicon-edit"></i> Edit Profile'.html_safe, edit_user_registration_path, class: 'dropmenu-item-btn') %></li>
                <li><%= link_to('<i class="glyphicon glyphicon-comment"></i> Notifications'.html_safe, '#', class: 'dropmenu-item-btn') %></li>
                <li class="divider"></li>
                <li><%= link_to('<i class="glyphicon glyphicon-log-out"></i> Log Out  '.html_safe, destroy_user_session_path, method: 'delete', class: 'dropmenu-item-btn') %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to 'Log In', new_user_session_path %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <%= locale %> <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% alternate_locales do |l| %>
                <li class="language <%= l %>"><%= link_to(l, "/"+l.to_s ) %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>