<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to('TryFree', root_path, class: 'navbar-brand')  %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Page 1</a></li>
        <li><a href="#">Page 2</a></li>
        <li><a href="#">Page 3</a></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li><%= link_to "Nyt Blogindlæg", root_path %></li>
        <li><%= link_to "Ny opskrift", root_path %> </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span>Konto
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Profil', '#' %></li>
            <li><%= link_to 'Indstillinger', edit_user_registration_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to 'Log Ud', destroy_user_session_path, method: 'delete' %>
            </li>
          </ul>
        </li>
        <% else %>
          <li><%= link_to 'Log In', new_user_session_path %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <%= locale %> <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% alternate_locales do |l| %>
              <li class="language <%= l %>"><%= link_to(l, "/"+l.to_s ) %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
