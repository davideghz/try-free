// Add the dialog title
$('#dialog h3').html("<i class='glyphicon glyphicon-info-sign'></i> Lesson information");

// Render the partial
$('.modal-body').html('<%= j render("lesson_info") %>');

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
  $('.first_input').focus()
})