// Add the dialog title
$('#dialog h4').html("<i class='glyphicon glyphicon-info-sign'></i> Lesson of: <%= @lesson.topic.title %>");

// Render the partial
$('.modal-body').html('<%= j render("lesson_info") %>');

// Show the dynamic dialog
$('#dialog').modal("show");